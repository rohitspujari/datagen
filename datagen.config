# Interval in Seconds
interval = 1

# Regex expression to extract existing timestamp token from sample events
timestamp_token = '\d{2}\/\w{3}\/\d{4}\s\d{2}:\d{2}:\d{2}'

# Strftime format of replaced timestamp_token
timestamp_format = '%d/%b/%Y %H:%M:%S'
